var config = {};
config.port = process.env.WEB_PORT || 8000;
config.couch = {
  auth: {
    username: 'username',
    password: 'password'
  },
  host : '127.0.0.1',
  port : 5984,
  name : 'databasename'
}

module.exports = config;
